


	  <div class='col-md-3'>

<h2>Uers info</h2>
<table class='table'>
  <tr>
  <%= image_tag user_id.get_profile_image(100,100) %>
   </tr>
   <tr>
  <th>name</th>
  <th><%= user_id.name %></th>
  </tr>
  <tr>
  <th>introduction</th>
  <th><%= user_id.introduction %></th>
  </tr>
  </table>

  <div class='row'>
  <li>
<%= link_to edit_user_path(user_id.id), class:"svg-inline--fa fa-user-gear" do%>
<i class="fas fa-user-cog"></i>
	<% end %>
	</li>
  </div>


  <h2 class="mt-3">New book</h2>
<% if book.errors.any? %>
  <%= book.errors.count %>errors prohibited this obj from being saved:
  <ul>
    <% book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_with model: book,url: '/books',method: :post do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Opinion</p>
  <%= f.text_area :body %>
  <br />
  <div class="actions">
  <%= f.submit "Create Book", class: "btn btn-success" %>
  </div>
<% end %>

</div>
